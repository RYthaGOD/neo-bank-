[phases.setup]
nixPkgs = ["nodejs_22", "python3", "pkg-config", "openssl", "udev"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
